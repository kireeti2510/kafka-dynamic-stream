#!/bin/bash
# ═══════════════════════════════════════════════════════════════
#  SYSTEM 3: CONSUMER - TERMINAL COMMANDS
# ═══════════════════════════════════════════════════════════════

echo "═══════════════════════════════════════════════════════════"
echo "  SYSTEM 3: CONSUMER SETUP COMMANDS"
echo "═══════════════════════════════════════════════════════════"
echo ""
echo "Copy and paste these commands in order:"
echo ""

cat << 'EOF'

# ──────────────────────────────────────────────────────────────
# STEP 1: Get Your System's IP Address
# ──────────────────────────────────────────────────────────────
hostname -I | awk '{print $1}'

# ✏️ WRITE DOWN:
# System 3 (This System) IP: ________________
# System 2 (Kafka Broker) IP: ________________
# System 4 (Admin/DB) IP: ________________
# System 4 Username: ________________


# ──────────────────────────────────────────────────────────────
# STEP 2: Clone Repository
# ──────────────────────────────────────────────────────────────
cd ~
git clone https://github.com/kireeti2510/kafka-dynamic-stream.git
cd kafka-dynamic-stream


# ──────────────────────────────────────────────────────────────
# STEP 3: Edit Setup Script with Your IP Addresses
# ──────────────────────────────────────────────────────────────
nano distributed_configs/setup_system3_consumer.sh

# ✏️ EDIT THESE LINES (around line 10-13):
# SYSTEM2_IP="192.168.1.20"      ← Change to System 2 IP
# SYSTEM4_IP="192.168.1.40"      ← Change to System 4 IP
# SYSTEM4_USER="your_username"   ← Change to System 4 username

# Press: Ctrl+O (save), Enter, Ctrl+X (exit)


# ──────────────────────────────────────────────────────────────
# STEP 4: Run Setup Script
# ──────────────────────────────────────────────────────────────
cd distributed_configs
chmod +x setup_system3_consumer.sh
./setup_system3_consumer.sh

# ⏳ Wait for setup to complete...
# ✅ Setup will:
#    - Install Python dependencies
#    - Mount shared database from System 4
#    - Create config.json
#    - Test connectivity


# ──────────────────────────────────────────────────────────────
# STEP 5: Verify Setup
# ──────────────────────────────────────────────────────────────
# Test Kafka connectivity
telnet SYSTEM2_IP 9092
# Press Ctrl+] then type 'quit' to exit

# Test database mount
ls -la /mnt/shared_db/

# If database mount fails, manually mount:
# sudo mount SYSTEM4_IP:/home/SYSTEM4_USER/shared_db /mnt/shared_db


# ──────────────────────────────────────────────────────────────
# STEP 6: Start Consumer (User ID 1)
# ──────────────────────────────────────────────────────────────
cd ~/kafka-dynamic-stream
source venv/bin/activate
cd consumer
python3 consumer.py 1

# ──────────────────────────────────────────────────────────────
# CONSUMER COMMANDS (Once Running):
# ──────────────────────────────────────────────────────────────
# list                              - List all active topics
# subscribed                        - Show your subscribed topics
# subscribe <topic1> <topic2> ...   - Subscribe to topics
# unsubscribe <topic1> ...          - Unsubscribe from topics
# refresh                           - Refresh subscriptions
# help                              - Show help
# quit                              - Exit


# ──────────────────────────────────────────────────────────────
# RUNNING MULTIPLE CONSUMERS (Optional)
# ──────────────────────────────────────────────────────────────
# Open a new terminal on System 3:

# Terminal 1 - Consumer User 1:
cd ~/kafka-dynamic-stream
source venv/bin/activate
cd consumer
python3 consumer.py 1

# Terminal 2 - Consumer User 2:
cd ~/kafka-dynamic-stream
source venv/bin/activate
cd consumer
python3 consumer.py 2

# Terminal 3 - Consumer User 3:
cd ~/kafka-dynamic-stream
source venv/bin/activate
cd consumer
python3 consumer.py 3


# ──────────────────────────────────────────────────────────────
# TESTING:
# ──────────────────────────────────────────────────────────────
# 1. List active topics:
#    > list
#
# 2. Subscribe to a topic:
#    > subscribe news_updates
#
# 3. Wait for messages from System 1 (Producer)
#
# 4. When message arrives, you'll see:
#    📨 [news_updates] Message received:
#       Content: Hello from distributed system!
#       Timestamp: 2025-11-06 10:30:45
#       Partition: 1 | Offset: 0

EOF
